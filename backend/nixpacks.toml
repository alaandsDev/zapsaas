[phases.setup]
nixPkgs = ["nodejs_22", "python3", "pkg-config", "cairo", "pango", "libpng", "libjpeg", "giflib"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[start]
cmd = "node server.js"
